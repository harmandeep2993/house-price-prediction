project:
  name: "house price prediction random forest"
  author: "Harmandeep Singh"
  version: "1.0"

data:
  raw_dir: "data/raw"
  processed_dir: "data/processed"
  train_file: "train.csv"
  test_file: "test.csv"

model:
  model_dir: "models"
  model_file: "rf_model.pkl"
  columns_file: "columns.json"

evaluation:
  metrics_dir: "results"
  metrics_file: "metrics.json"
  save_plots: true

app:
  model_path: "models/rf_model.pkl"
  host: "localhost"
  port: 8501